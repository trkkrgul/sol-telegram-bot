export const getSolscanAccountLink = (address) => {
  return `https://solscan.io/account/${address}`;
};

export const getSolscanTxLink = (signature) => {
  return `https://solscan.io/tx/${signature}`;
};
